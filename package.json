{
  "name": "polarity",
  "version": "1.0.0",
  "description": "Polarity - an endless one-tap reflex game",
  "private": true,
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
